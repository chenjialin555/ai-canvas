# 第二课：Props 父子组件传值

这一课我们只学一个东西：

```text
props
```

你项目里大量代码看不懂，主要就是因为 props。

比如你项目里的：

```tsx
<AppShell
  topBar={<TopBar />}
  leftSidebar={<LeftSidebar />}
  canvasArea={<CanvasArea />}
  rightSidebar={<RightSidebar />}
  overlays={<EditorModals />}
/>
```

还有：

```tsx
<CanvasArea
  stageRef={stageRef}
  onOpenCropEditor={(id) => modals.setCropEditorImageId(id)}
/>
```

这些都是 props。

---

# 1. props 是什么？

一句话：

```text
props 就是父组件传给子组件的数据或函数。
```

Vue 里你也用过 props。

Vue：

```vue
<UserCard name="小明" age="18" />
```

React：

```tsx
<UserCard name="小明" age={18} />
```

子组件收到：

```text
name = 小明
age = 18
```

---

# 2. 为什么需要 props？

因为组件要复用。

比如你想显示很多用户卡片：

```text
小明 18岁
小红 20岁
小刚 22岁
```

你不想写三个不同组件：

```tsx
<XiaoMingCard />
<XiaoHongCard />
<XiaoGangCard />
```

而是写一个通用组件：

```tsx
<UserCard name="小明" age={18} />
<UserCard name="小红" age={20} />
<UserCard name="小刚" age={22} />
```

这就是 props 的作用。

---

# 3. 最简单的 props 例子

```tsx
function UserCard(props: { name: string; age: number }) {
  return (
    <div>
      <h2>{props.name}</h2>
      <p>年龄：{props.age}</p>
    </div>
  );
}

export function App() {
  return (
    <div>
      <UserCard name="小明" age={18} />
      <UserCard name="小红" age={20} />
    </div>
  );
}
```

---

## 人话解释

```tsx
<UserCard name="小明" age={18} />
```

父组件传给子组件：

```text
name 是 小明
age 是 18
```

子组件里用：

```tsx
props.name
props.age
```

显示出来。

---

# 4. Vue 对比

Vue 子组件：

```vue
<script setup>
defineProps({
  name: String,
  age: Number
})
</script>

<template>
  <div>
    <h2>{{ name }}</h2>
    <p>年龄：{{ age }}</p>
  </div>
</template>
```

React 子组件：

```tsx
function UserCard(props: { name: string; age: number }) {
  return (
    <div>
      <h2>{props.name}</h2>
      <p>年龄：{props.age}</p>
    </div>
  );
}
```

对照：

| Vue | React |
|---|---|
| `defineProps` | 函数参数 `props` |
| `{{ name }}` | `{props.name}` |
| `:age="18"` | `age={18}` |

---

# 5. props 可以传字符串、数字、布尔值

```tsx
function Product(props: {
  title: string;
  price: number;
  available: boolean;
}) {
  return (
    <div>
      <h2>{props.title}</h2>
      <p>价格：{props.price}</p>
      <p>{props.available ? "有货" : "无货"}</p>
    </div>
  );
}

export function App() {
  return (
    <Product
      title="苹果"
      price={5}
      available={true}
    />
  );
}
```

注意：

```tsx
title="苹果"
```

字符串可以直接写。

数字和布尔值要用 `{}`：

```tsx
price={5}
available={true}
```

---

# 6. props 可以传函数

这是你项目里最常见的。

比如：

```tsx
function Button(props: { onClick: () => void }) {
  return (
    <button onClick={props.onClick}>
      点击
    </button>
  );
}

export function App() {
  return (
    <Button
      onClick={() => alert("父组件传来的函数")}
    />
  );
}
```

---

## 人话解释

父组件说：

```text
Button，你被点击时，帮我执行这个函数。
```

子组件：

```tsx
<button onClick={props.onClick}>
```

意思是：

```text
按钮被点击时，执行父组件传来的 props.onClick。
```

---

# 7. Vue 对比函数 props / emit

Vue 里你可能会写：

子组件：

```vue
<button @click="$emit('open')">
  打开
</button>
```

父组件：

```vue
<Child @open="openModal" />
```

React 里通常不叫 emit，而是直接传函数：

父组件：

```tsx
<Child onOpen={openModal} />
```

子组件：

```tsx
function Child(props: { onOpen: () => void }) {
  return (
    <button onClick={props.onOpen}>
      打开
    </button>
  );
}
```

所以：

```text
Vue 的 emit
≈ React 的回调函数 props
```

---

# 8. 子组件可以传参数给父组件

比如子组件有一个 id，点击时告诉父组件：

```tsx
function ImageButton(props: {
  imageId: string;
  onOpenCrop: (id: string) => void;
}) {
  return (
    <button onClick={() => props.onOpenCrop(props.imageId)}>
      裁剪图片
    </button>
  );
}

export function App() {
  return (
    <ImageButton
      imageId="img-001"
      onOpenCrop={(id) => {
        console.log("要裁剪的图片是：", id);
      }}
    />
  );
}
```

---

## 这和你的项目很像

你的项目里有：

```tsx
<CanvasArea
  onOpenCropEditor={(id) => modals.setCropEditorImageId(id)}
/>
```

意思是：

```text
CanvasArea 里面如果要打开裁剪编辑器，
就调用 onOpenCropEditor(id)，
App 收到 id 后，设置 cropEditorImageId。
```

---

# 9. props 可以传 ReactNode

这就是你项目里的 AppShell。

先看一个小例子：

```tsx
import type { ReactNode } from "react";

function Layout(props: {
  header: ReactNode;
  content: ReactNode;
  footer: ReactNode;
}) {
  return (
    <div>
      <header>{props.header}</header>
      <main>{props.content}</main>
      <footer>{props.footer}</footer>
    </div>
  );
}

export function App() {
  return (
    <Layout
      header={<h1>顶部</h1>}
      content={<p>中间内容</p>}
      footer={<small>底部</small>}
    />
  );
}
```

---

## 人话解释

父组件传：

```tsx
header={<h1>顶部</h1>}
```

子组件显示：

```tsx
<header>{props.header}</header>
```

意思是：

```text
父组件决定 header 放什么。
Layout 只负责摆位置。
```

---

# 10. 这就是你的 AppShell

你的项目：

```tsx
type AppShellProps = {
  onRootMouseDown: () => void;
  topBar: ReactNode;
  leftSidebar: ReactNode;
  canvasArea: ReactNode;
  rightSidebar: ReactNode;
  overlays: ReactNode;
};
```

意思是：

```text
AppShell 需要父组件传：
- 一个鼠标按下事件函数
- 顶部栏内容
- 左侧栏内容
- 画布区域内容
- 右侧栏内容
- 浮层内容
```

然后：

```tsx
export function AppShell(props: AppShellProps) {
  return (
    <div className="figma-app" onMouseDown={props.onRootMouseDown}>
      {props.topBar}
      <main className="figma-main">
        {props.leftSidebar}
        {props.canvasArea}
        {props.rightSidebar}
      </main>
      {props.overlays}
    </div>
  );
}
```

意思是：

```text
AppShell 不知道 topBar 里面是什么。
它只知道：我要把 props.topBar 放在顶部。
```

---

# 11. App.tsx 怎么使用 AppShell？

你的项目里：

```tsx
<AppShell
  onRootMouseDown={() => {
    if (modals.contextMenu.visible) {
      modals.setContextMenu((m) => ({ ...m, visible: false }));
    }
  }}
  topBar={
    <TopBar
      onOpenLibrary={() => modals.setLibraryOpen(true)}
      onOpenAi={() => modals.setAiOpen(true)}
      exportStage={exportStage}
    />
  }
  leftSidebar={<LeftSidebar />}
  canvasArea={
    <CanvasArea
      stageRef={stageRef}
      onOpenCropEditor={(id) => modals.setCropEditorImageId(id)}
    />
  }
  rightSidebar={<RightSidebar />}
  overlays={
    <>
      <ContextMenu />
      <EditorModals />
    </>
  }
/>
```

你现在可以分开看：

---

## 11.1 onRootMouseDown

```tsx
onRootMouseDown={() => {
  if (modals.contextMenu.visible) {
    modals.setContextMenu((m) => ({ ...m, visible: false }));
  }
}}
```

意思：

```text
传给 AppShell 一个函数。
AppShell 最外层 div 被鼠标按下时，执行这个函数。
这个函数会关闭右键菜单。
```

---

## 11.2 topBar

```tsx
topBar={
  <TopBar
    onOpenLibrary={() => modals.setLibraryOpen(true)}
    onOpenAi={() => modals.setAiOpen(true)}
    exportStage={exportStage}
  />
}
```

意思：

```text
topBar 区域放 TopBar 组件。
同时给 TopBar 传几个按钮回调。
```

---

## 11.3 canvasArea

```tsx
canvasArea={
  <CanvasArea
    stageRef={stageRef}
    onOpenCropEditor={(id) => modals.setCropEditorImageId(id)}
  />
}
```

意思：

```text
canvasArea 区域放 CanvasArea 组件。
如果 CanvasArea 想打开裁剪编辑器，就调用 onOpenCropEditor(id)。
App 收到 id 后设置弹窗状态。
```

---

## 11.4 overlays

```tsx
overlays={
  <>
    <ContextMenu />
    <EditorModals />
  </>
}
```

意思：

```text
overlays 区域放多个浮层：
- 右键菜单
- 各种弹窗
```

---

# 12. props 传值方向

props 是从父传子。

```text
父组件
  ↓ props
子组件
```

比如：

```text
App
  ↓ topBar / canvasArea / overlays
AppShell
```

还有：

```text
App
  ↓ onOpenCropEditor
CanvasArea
```

---

# 13. 子组件怎么通知父组件？

React 里通常通过函数 props。

```text
父组件传一个函数给子组件
子组件在合适的时候调用这个函数
```

例如：

```tsx
function Child(props: { onOpen: () => void }) {
  return <button onClick={props.onOpen}>打开</button>;
}

function Parent() {
  return (
    <Child
      onOpen={() => console.log("子组件通知我了")}
    />
  );
}
```

流程：

```text
父组件 Parent
↓ 把 onOpen 函数传给 Child
Child 用户点击按钮
↓ 调 props.onOpen()
Parent 的函数执行
```

Vue 对比：

```text
React 函数 props
≈
Vue emit
```

---

# 14. props 不应该被子组件直接修改

React 里 props 是只读的。

比如：

```tsx
function UserCard(props: { name: string }) {
  props.name = "小红"; // 不要这样
  return <div>{props.name}</div>;
}
```

正确做法是：

```text
如果子组件想改变父组件的数据，
就调用父组件传来的函数。
```

例如：

```tsx
function Child(props: { onChangeName: (name: string) => void }) {
  return (
    <button onClick={() => props.onChangeName("小红")}>
      改名
    </button>
  );
}
```

---

# 15. props 和 state 区别

| 名称 | 谁拥有 | 谁能改 |
|---|---|---|
| props | 父组件传来 | 子组件不能直接改 |
| state | 组件自己拥有 | 组件自己用 setState 改 |
| store | 全局拥有 | 通过 store action 改 |

---

# 16. 在你的项目里，这三类都存在

## props

```tsx
<CanvasArea
  stageRef={stageRef}
  onOpenCropEditor={(id) => modals.setCropEditorImageId(id)}
/>
```

App 传给 CanvasArea。

---

## state / hook 状态

```tsx
const modals = useAppModals();
```

管理弹窗、右键菜单、toast。

---

## store

```tsx
const selectedIds = useEditorStore((s) => s.selectedIds);
```

全局编辑器状态。

---

# 17. TypeScript 里的 props 类型

React + TypeScript 经常这样写：

```tsx
type UserCardProps = {
  name: string;
  age: number;
};

function UserCard(props: UserCardProps) {
  return (
    <div>
      {props.name} - {props.age}
    </div>
  );
}
```

也可以直接写：

```tsx
function UserCard(props: { name: string; age: number }) {
  return <div>{props.name}</div>;
}
```

你的项目一般用单独的 type，比如：

```tsx
type AppShellProps = {
  topBar: ReactNode;
  canvasArea: ReactNode;
};
```

---

# 18. 解构 props

有时候会看到：

```tsx
function UserCard({ name, age }: UserCardProps) {
  return (
    <div>
      {name} - {age}
    </div>
  );
}
```

这等于：

```tsx
function UserCard(props: UserCardProps) {
  return (
    <div>
      {props.name} - {props.age}
    </div>
  );
}
```

只是写法更短。

---

# 19. 你的 AppShell 如果解构可以这样写

现在是：

```tsx
export function AppShell(props: AppShellProps) {
  return (
    <div>
      {props.topBar}
      {props.canvasArea}
    </div>
  );
}
```

也可以写成：

```tsx
export function AppShell({
  onRootMouseDown,
  topBar,
  leftSidebar,
  canvasArea,
  rightSidebar,
  overlays,
}: AppShellProps) {
  return (
    <div className="figma-app" onMouseDown={onRootMouseDown}>
      {topBar}
      <main className="figma-main">
        {leftSidebar}
        {canvasArea}
        {rightSidebar}
      </main>
      {overlays}
    </div>
  );
}
```

两种都可以。

---

# 20. 你项目里的 props 类型怎么读？

看到：

```tsx
type AppShellProps = {
  onRootMouseDown: () => void;
  topBar: ReactNode;
};
```

你就读成：

```text
AppShell 需要两个 props：
1. onRootMouseDown：一个没有参数、没有返回值的函数
2. topBar：一块可以显示的 React 内容
```

---

# 21. 函数类型怎么读？

```ts
onOpen: () => void;
```

意思：

```text
onOpen 是一个函数
它没有参数
它不返回东西
```

---

```ts
onOpenCropEditor: (id: string) => void;
```

意思：

```text
onOpenCropEditor 是一个函数
它接收一个 string 类型的 id
它不返回东西
```

对应你的项目：

```tsx
onOpenCropEditor={(id) => modals.setCropEditorImageId(id)}
```

意思：

```text
拿到 id 后，设置裁剪弹窗正在编辑的图片 id。
```

---

# 22. ReactNode 类型怎么读？

```ts
topBar: ReactNode;
```

意思：

```text
topBar 可以是一个 React 能显示的内容。
```

比如：

```tsx
<TopBar />
<div>顶部</div>
null
```

---

# 23. 这节课对应你的真实代码

现在你再看：

```tsx
<AppShell
  topBar={<TopBar />}
  leftSidebar={<LeftSidebar />}
  canvasArea={<CanvasArea />}
  rightSidebar={<RightSidebar />}
  overlays={
    <>
      <ContextMenu />
      <EditorModals />
    </>
  }
/>
```

你应该能读成：

```text
App 把这些区域作为 props 传给 AppShell。
AppShell 负责把它们摆在页面上。
```

再看：

```tsx
<CanvasArea
  onOpenCropEditor={(id) => modals.setCropEditorImageId(id)}
/>
```

你应该读成：

```text
App 传给 CanvasArea 一个函数。
CanvasArea 以后如果想打开裁剪弹窗，就调用这个函数，并传图片 id。
```

---

# 24. 练习 1：简单 props

```tsx
type HelloProps = {
  name: string;
};

function Hello(props: HelloProps) {
  return <h1>你好，{props.name}</h1>;
}

export function Demo() {
  return (
    <div>
      <Hello name="小明" />
      <Hello name="小红" />
    </div>
  );
}
```

你要能说出：

```text
Demo 是父组件
Hello 是子组件
Demo 通过 name 给 Hello 传值
Hello 通过 props.name 使用这个值
```

---

# 25. 练习 2：函数 props

```tsx
type ChildProps = {
  onSayHello: () => void;
};

function Child(props: ChildProps) {
  return (
    <button onClick={props.onSayHello}>
      说你好
    </button>
  );
}

export function Parent() {
  return (
    <Child
      onSayHello={() => alert("你好")}
    />
  );
}
```

你要能说出：

```text
Parent 传了一个函数给 Child
Child 按钮点击时调用这个函数
所以 alert 出现
```

---

# 26. 练习 3：带参数的函数 props

```tsx
type ImageButtonProps = {
  imageId: string;
  onCrop: (id: string) => void;
};

function ImageButton(props: ImageButtonProps) {
  return (
    <button onClick={() => props.onCrop(props.imageId)}>
      裁剪
    </button>
  );
}

export function Demo() {
  return (
    <ImageButton
      imageId="img-001"
      onCrop={(id) => console.log("裁剪图片：", id)}
    />
  );
}
```

这个和你的项目非常像：

```tsx
onOpenCropEditor={(id) => modals.setCropEditorImageId(id)}
```

---

# 27. 练习 4：ReactNode props

```tsx
import type { ReactNode } from "react";

type SimpleLayoutProps = {
  header: ReactNode;
  body: ReactNode;
};

function SimpleLayout(props: SimpleLayoutProps) {
  return (
    <div>
      <header>{props.header}</header>
      <main>{props.body}</main>
    </div>
  );
}

export function Demo() {
  return (
    <SimpleLayout
      header={<h1>这是顶部</h1>}
      body={<p>这是内容</p>}
    />
  );
}
```

这和 AppShell 一样。

---

# 28. 小测验

## 1. props 是谁传给谁？

A. 子组件传给父组件  
B. 父组件传给子组件

---

## 2. React 里 Vue 的 emit 通常用什么代替？

A. CSS  
B. 函数 props  
C. localStorage

---

## 3. 这句什么意思？

```tsx
<Hello name="小明" />
```

---

## 4. 这句什么意思？

```tsx
<Button onClick={() => alert("hi")} />
```

---

## 5. 这句什么意思？

```tsx
<AppShell topBar={<TopBar />} />
```

---

# 29. 本课总结

今天你只要记住：

```text
props = 父组件传给子组件的数据或函数
```

分几种：

```text
1. 普通数据 props
   <UserCard name="小明" age={18} />

2. 函数 props
   <Button onClick={() => ...} />

3. ReactNode props
   <AppShell topBar={<TopBar />} />
```

以及：

```text
Vue emit ≈ React 函数 props
```

---

# 30. 下一课建议

下一课学：

# 第三课：useState 组件自己的状态

因为你的项目里：

```tsx
const modals = useAppModals();
```

本质上就是封装了很多 `useState`。

你学会 `useState` 后，就会理解：

```text
弹窗为什么能打开/关闭
右键菜单为什么能显示/隐藏
toast 为什么几秒后消失
```

下一课我们就学：

```text
useState
状态改变
条件渲染
弹窗开关
```